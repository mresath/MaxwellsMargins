#include "electrostatics/EquipotentialTracer.hpp"

// TODO(Phase 2): implement marching-squares contour extraction over the potential grid.

std::vector<std::vector<Vec2>> EquipotentialTracer::traceContours(const std::vector<PointCharge> &charges, float potentialValue) const
{
    (void)charges;
    (void)potentialValue;
    return {};
}
